# tcsd TPM daemon configuration (demo)
# Requires tpm2-tools and a TPM 2.0 device at /dev/tpm0
port = 30003
num_threads = 10
system_ps_file = /var/lib/tpm/system.data
# See tpm2-tss documentation for production configuration
